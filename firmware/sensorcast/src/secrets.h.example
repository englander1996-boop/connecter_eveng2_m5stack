// Wi-Fi 接続情報のテンプレート。
// この内容を同じフォルダの secrets.h にコピーして、自分の Wi-Fi に書き換える。
// secrets.h は手元だけのファイル(チャットやリポジトリに出さない)。
#pragma once

#define WIFI_SSID "chocolate"
#define WIFI_PASS "ここに chocolate のパスワード"
